#DB Config
#Heroku
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}
spring.jpa.show-sql=false
spring.jpa.generate-ddl=true

#Local
#spring.datasource.url= jdbc:postgresql://localhost:5432/securityreact
#spring.datasource.username=postgres
#spring.datasource.password=PostgreSQL
#spring.jpa.generate-ddl=true

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation= true
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto= update

# App Properties
ken.jwtSecret= kenSecretKey
ken.jwtExpirationMs= 86400000

#Auth
spring.mail.host = smtp.elasticemail.com
spring.mail.port = 2525
spring.mail.username = AuthenticationService@ken.com
spring.mail.password = 428C7D672868DDB99EAF0105CD809FF46CE5

